<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        />

    <TextView
        android:id="@+id/txt_time"
        android:layout_width="@dimen/classroom_50dp"
        android:layout_height="@dimen/classroom_30dp"
        android:layout_marginTop="@dimen/classroom_10dp"
        android:layout_marginRight="@dimen/classroom_10dp"
        android:background="@drawable/start_bg"
        android:gravity="center"

        android:text="跳过3"
        android:textSize="@dimen/classroom_10dp"
        android:textColor="@color/result_text"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    <TextView

        android:id="@+id/start_tv"
        android:layout_width="match_parent"
        android:layout_height="46dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="50dp"
        android:background="@drawable/start_bg"
        android:gravity="center"
        android:text="立即体验"
        android:textColor="#ffffff"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_goneMarginBottom="@dimen/classroom_80dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>